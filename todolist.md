# 项目待办清单

> 说明：本清单汇总当前项目中尚未完成、建议继续推进或需要重做的事项，按优先级从高到低排列。

## 一、最高优先级：修正实验口径与数据泄露问题

- [x] 梳理“贷前可用字段白名单”，只保留申请时可获得的输入特征
- [x] 梳理“必须删除的泄露字段黑名单”，重点排查并移除以下字段：
  - `loan_status`
  - `last_pymnt_d`
  - `last_pymnt_amnt`
  - `next_pymnt_d`
  - `last_credit_pull_d`
  - `recoveries`
  - `collection_recovery_fee`
  - `hardship_*`
  - `settlement_*`
  - 其他明显贷后字段
- [x] 修改 `src/features/preprocessor.py`，确保预处理默认排除所有贷后泄露字段
- [x] 复查 `src/label_builder.py` 中标签定义逻辑，明确“标签可来自历史表现，但模型输入必须只用贷前字段”
- [x] 重新确认“贷前风险分级”任务定义，避免任务口径与特征口径不一致

## 二、高优先级：重做风险分类实验

- [ ] 使用去泄露后的特征重新生成训练集、验证集、测试集输入
- [ ] 重新训练风险分类模型：
  - 逻辑回归
  - 决策树
  - 随机森林
  - XGBoost
  - LightGBM
- [ ] 重新执行超参数调优与时序交叉验证
- [ ] 重新生成风险分类评估结果：
  - 混淆矩阵
  - ROC 曲线
  - PR 曲线
  - 分类报告
- [ ] 补充并重点关注以下指标：
  - `macro F1`
  - 各类别 `precision`
  - 各类别 `recall`
  - 各类别 `f1-score`
  - `balanced accuracy`
  - 少数类召回率
- [ ] 单独分析风险类样本的识别效果，避免只看总体准确率
- [ ] 重新评估 `risk_model_confusion_matrix.png` 是否仍存在“几乎全判为 0 类”的问题

## 三、高优先级：重做额度测算实验

- [ ] 使用去泄露后的特征重新训练额度测算模型
- [ ] 重新比较以下回归模型：
  - 线性回归
  - Ridge
  - Lasso
  - LightGBM
- [ ] 重新执行额度模型调参与测试集评估
- [ ] 重新生成回归评估图：
  - `credit_scoring_prediction_vs_actual.png`
  - `credit_scoring_error_distribution.png`
- [ ] 补充并重点关注以下指标：
  - `MAE`
  - `MAPE`
  - `RMSE`
  - `R²`
  - 分风险等级分层误差
- [ ] 重新验证监管硬约束逻辑是否稳定生效
- [ ] 评估“约束前额度”和“约束后额度”的差异分布

## 四、高优先级：修正风控指标口径

- [ ] 重新定义 `KS` 的业务计算口径
- [ ] 确定坏样本定义，例如：
  - 仅 `4` 类为坏样本
  - `3/4` 类为坏样本
  - `2/3/4` 类为风险样本
- [ ] 在二分类坏样本口径下重新计算 `KS`
- [ ] 重新计算不良率捕捉率 / 坏样本捕捉率
- [ ] 在论文、README、PPT 中避免直接使用当前多分类 one-vs-rest `KS` 作为最终主指标

## 五、中优先级：重做可解释性分析

- [ ] 在去泄露后的模型上重新运行 SHAP 分析
- [ ] 重新生成：
  - `shap_summary_plot.png`
  - `shap_force_plot.html`
  - `shap_feature_importance.csv`
- [ ] 检查 Top 特征是否仍然出现明显贷后字段
- [ ] 将最终可解释性结论改为真正可用于贷前风控解释的版本

## 六、中优先级：区分 Demo 指标与正式实验指标

- [ ] 明确当前 `src/models/artifacts/dashboard_metrics.json` 是否为 demo 级指标
- [ ] 若继续保留 demo 自举训练逻辑，在文档中明确说明“仅用于演示”
- [ ] 增加“正式实验结果”和“Demo 演示结果”的区分口径
- [ ] 视需要将 `src/pipeline.py` 中的 demo 级训练样本规模参数做成可配置项

## 七、中优先级：补充结果与数据产物

- [ ] 重新生成并保留 `data/processed/train.csv`、`val.csv`、`test.csv`
- [ ] 重新生成并保留特征工程输出的 `.npy` 文件
- [ ] 核对 `results/figures/` 中每张图是否来自最新实验
- [ ] 清理已过时或口径不正确的图表结果，避免提交时混用旧结果

## 八、中优先级：更新文档与答辩材料

- [ ] 根据重跑后的正式指标更新 `README.md`
- [ ] 根据重跑后的正式指标更新 `docs/RUN_GUIDE.md`
- [ ] 根据重跑后的正式指标更新 `docs/PAPER_OUTLINE.md`
- [ ] 根据重跑后的正式指标更新 `docs/PPT_OUTLINE.md`
- [ ] 在 PPT 中替换当前暂用指标：
  - 分类准确率 `0.9918`
  - 加权 F1 `0.9892`
  - KS `0.9893`
  - 回归 `R² = 0.0895`
  - 回归 `RMSE = 8964.10`
- [ ] 在论文中补充“实验局限性”和“数据泄露修正说明”

## 九、中优先级：完善课程提交材料

- [ ] 将 `docs/CONTRIBUTION.md` 中的“成员A / 成员B / 成员C / 成员D”替换为真实姓名
- [ ] 根据实际情况调整团队贡献比例
- [ ] 将 `docs/PAPER_OUTLINE.md` 扩展为论文初稿
- [ ] 将 `docs/PPT_OUTLINE.md` 扩展为逐页讲稿版
- [ ] 整理最终提交清单：
  - 代码
  - 文档
  - 图表
  - Notebook
  - 演示材料

## 十、可选优化项

- [ ] 将首页架构图中的英文类名进一步改成更业务化的中文节点
- [ ] 为批量上传功能提供“中文列名模板 + 自动映射”
- [ ] 增加命令行训练入口脚本，替代手动运行 Notebook
- [ ] 将模型训练配置参数统一抽到配置文件
- [ ] 增加正式的实验日志记录和版本标记机制

## 十一、建议执行顺序

- [ ] 第一步：先修正特征口径与泄露字段问题
- [ ] 第二步：重跑风险分类实验
- [ ] 第三步：重跑额度测算实验
- [ ] 第四步：重做 SHAP 与指标分析
- [ ] 第五步：更新 README、运行文档、论文大纲和 PPT 大纲
- [ ] 第六步：整理最终提交材料与答辩版本
